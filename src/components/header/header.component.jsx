import React from "react";

import DenseAppBar from "./app-bar/app.bar.component";

const Header = () => {
  return (
    <div id="app-footer">
      <DenseAppBar />
    </div>
  );
};

export default Header;
